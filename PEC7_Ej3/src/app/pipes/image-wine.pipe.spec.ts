import { ImageWinePipe } from './image-wine.pipe';

describe('ImageWinePipe', () => {
  it('create an instance', () => {
    const pipe = new ImageWinePipe();
    expect(pipe).toBeTruthy();
  });
});
